{
  "version": 2,
  "buildCommand": "npm run build && rm -rf client && mv client/dist/client .",
  "outputDirectory": "client",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
