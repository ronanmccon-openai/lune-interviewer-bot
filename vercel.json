{
  "version": 2,
  "buildCommand": "npm run build && rm -rf client-build && cp -r client/dist/client client-build",
  "outputDirectory": "client-build",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
